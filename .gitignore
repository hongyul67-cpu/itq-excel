# 작업 중 파일 — 배포본에 섞이면 안 되는 것들
_chk.js
_scratch/
node_modules/
*.log
.DS_Store
Thumbs.db

# 원자료(교과서 스캔·기출 PDF 등)는 공개 저장소에 올리지 않습니다.
_src/

# ── 교재 원문(평문) — 절대 커밋하지 말 것 ──────────────────
# 배포는 sets.enc / enc/*.enc 로 암호화해서만 한다.
# git 은 패턴 뒤 같은 줄의 주석을 인식하지 못한다. 주석은 반드시 따로 줄에.

# 회차별 전사 원문
sets/
# 교재에서 잘라낸 출력형태 그림
img/
# 코드표 백업
codes.txt

# sets_book.js 는 sets/ 를 모으는 조립기일 뿐이라 커밋한다.
__pycache__/
